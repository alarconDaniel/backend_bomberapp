import { Controller } from '@nestjs/common';
import { ApiTags } from '@nestjs/swagger';

/**
 * Endpoints relacionados con logros de usuario.
 * Por ahora es un placeholder para futuras operaciones sobre logros.
 */
@ApiTags('logros')
@Controller('logro')
export class LogroController {}
